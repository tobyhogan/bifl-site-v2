{
  "compilerOptions": {
    "checkJs": true,
    "allowJs": true,
    "target": "es2020",
    // ... other TypeScript compiler options
  },
  "include": [
    "./src/**/*", // Or the paths to your JavaScript files
    "./*.js"
  ],
  "exclude": [
    "node_modules",
    "tailwind.config.js" // Add this line to exclude your Tailwind config
  ]
}